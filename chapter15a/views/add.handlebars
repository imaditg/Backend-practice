<form id="addData">
    <table>
        <tr>
            <th>name:</th>
            <th>category:</th>
            <th>amount:</th>
            <th>available</th>
            <th class="empty"></th>
        </tr>
        <tr>
            <td><input type="text" name="name" required></td>
            <td><input type="text" name="category" required></td>
            <td><input type="number" name="price" required></td>
            <td class="available"><input type="text" placeholder="True" readonly disabled name="available" value="True">
            </td>
            <td><button class="subbtn" type="submit">Submit</button></td>
        </tr>
    </table>
</form>
<script>
    document.getElementById('addData').addEventListener('submit',(event)=>{
        event.preventDefault()
        const form = event.target
        const headers = {"Content-Type":"application/json"}
        const body = JSON.stringify({
            name:form.elements.name.value,
            category:form.elements.category.value,
            price:form.elements.price.value,
            available:form.elements.available.value,
        })
        fetch('/api/newadd',{method:"Post",headers,body})
        .then((res)=>{
            return res.json()
        })
        .then((res)=>{
            if(res.success){
                window.location.replace('/views/add/loading')
            }
            else{
                throw new Error('Not getting success')
            }
        })
        .catch((error)=>{
            console.log(`Error in adding new data ${error}`)
        })
    })
</script>